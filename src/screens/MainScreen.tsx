import { StyleSheet, View } from 'react-native';
import React from 'react';
import { Text } from '@components/common/Text';

export const MainScreen = () => {
  return (
    <View>
      <Text variant="body-sb">MainScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
